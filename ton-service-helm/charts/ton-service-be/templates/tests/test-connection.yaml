apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ton-service-be.fullname" . }}-test-connection"
  labels:
    {{- include "ton-service-be.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ton-service-be.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
