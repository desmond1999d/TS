apiVersion: v1
kind: Service
metadata:
  name: {{ include "ton-service-be.fullname" . }}
  labels:
    {{- include "ton-service-be.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ton-service-be.selectorLabels" . | nindent 4 }}
